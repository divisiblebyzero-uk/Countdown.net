import { WordSearchResults } from './word-search-results';

describe('WordSearchResults', () => {
  it('should create an instance', () => {
    expect(new WordSearchResults()).toBeTruthy();
  });
});
